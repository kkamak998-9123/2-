받는사람: opendata_help@nia.or.kr
제목: [B554287 지자체복지서비스] 엔드포인트·파라미터·인증키 정상 확인에도 모든 요청이 INVALID_REQUEST_PARAMETER_ERROR(코드 10) 반환 문의

안녕하세요.

한국사회보장정보원에서 제공하는 공공데이터포털 Open API
"지자체복지서비스"(서비스명: B554287, 데이터셋 15108347) 이용 중
모든 요청이 파라미터 값과 무관하게 아래와 같은 오류를 반환하여 문의드립니다.
아래에 원인을 좁히기 위해 직접 수행한 검증 결과를 함께 정리했습니다.

■ 활용신청 정보
- 활용신청자: [이름/기관명 입력]
- 신청 이메일: kkamak998@gmail.com
- 데이터명: 지자체복지서비스 (B554287, 15108347)
- 활용신청 승인일: 2026-08-03
- 문의 작성일: 2026-08-05 (승인 후 48시간 이상 경과, 여전히 동일 증상)

■ 증상
- 목록조회(LcgvWelfarelist), 상세조회(LcgvWelfaredetailed) 오퍼레이션 모두
  요청 파라미터를 정상적으로 넣거나(코드표 v1.0 기준 srchKeyCode/searchWrd/arrgOrd),
  아예 필터 파라미터를 전부 빼고 최소 파라미터만 넣어도 동일하게 다음 오류가 반환됩니다.

  HTTP 200
  resultCode: 10
  resultMessage: INVALID_REQUEST_PARAMETER_ERROR
  totalCount: 0

- 예시 요청(서비스키 마스킹):
  GET https://apis.data.go.kr/B554287/LocalGovernmentWelfareInformations/LcgvWelfarelist
      ?serviceKey=***&pageNo=1&numOfRows=5&srchKeyCode=003&searchWrd=아동&arrgOrd=001

■ 원인을 좁히기 위해 직접 확인한 사항 (문제 지점이 파라미터가 아님을 시사)
1) 엔드포인트/오퍼레이션명은 정상입니다.
   - 존재하지 않는 오퍼레이션명으로 호출하면 게이트웨이가 HTTP 400 +
     NO_OPENAPI_SERVICE_ERROR(reasonCode 12, "해당 오픈API 서비스가 없거나 폐기됨")를 반환합니다.
   - 반면 LcgvWelfarelist 는 HTTP 200 + 서비스 자체 응답(<wantedList>) 안의 resultCode 10을
     반환합니다. 즉 요청이 게이트웨이를 지나 실제 지자체복지서비스 백엔드까지 정상 도달하고 있으며,
     백엔드가 코드 10을 돌려주는 상황입니다.

2) 인증키/파라미터 자체의 문제가 아닙니다.
   - 동일 계정/동일 서비스키로 "중앙부처복지서비스"(B554287,
     NationalWelfareInformationsV001 / NationalWelfarelistV001, 데이터셋 15090532)에
     동일한 파라미터 조합(srchKeyCode=003, searchWrd=아동 등)을 보내면
     정상적으로 HTTP 200, resultCode 0(SUCCESS), totalCount 461 로 응답합니다.
   - 서비스키를 인코딩/디코딩 두 형태로 각각 보내도 지자체 API는 동일하게 코드 10입니다.
   - 따라서 서비스키·필수 파라미터·요청 형식의 문제가 아니라,
     지자체복지서비스(15108347) 백엔드에 제 계정의 활용신청 승인 정보가
     정상 반영되지 않은 것으로 추정됩니다.

■ 확인/조치 요청 사항
1. 지자체복지서비스(B554287, 15108347) API에 대한 제 계정(서비스키)의 활용신청 승인이
   실제 서비스 원장 서버(백엔드)에까지 정상 반영되었는지 확인 부탁드립니다.
2. 만약 승인은 정상 반영되었는데도 코드 10이 발생하는 것이라면,
   위 예시 요청 기준으로 어떤 파라미터가 유효하지 않은지, 정확한 필수 파라미터 목록과
   값 예시를 알려주시면 감사하겠습니다.

빠른 확인 부탁드립니다. 감사합니다.

[이름/기관명 입력]
연락처: [전화번호 입력]
